[package]
name = "build198x"
description = "The 198x family's build-tools pipeline — asset conversion, data packing, and media mastering for retro targets."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

# Lib + bin crate: the library carries the format codec modules (src/format/,
# per decisions/module-and-crate-naming.md); the binary stays a minimal stub
# until the CLI unit (subcommands, pipeline wiring). No third-party
# dependencies yet, by design.

[lints]
workspace = true
